@model IndividualProject_BookingWebApplication.Data.Models.User
@{
    ViewBag.Title = "Login";
}

<div class="container-fluid">
    <div class="row">
        <div id="side-img" class="col-md-6 d-none d-md-block p-0">
        </div>
        <div class="col-md-6 d-flex align-items-center justify-content-center">
            <div class="w-75 p-4 p-md-5">
                <h2 class="text-end">Login</h2>
                <!-- Parameters for beginform, Method name, controllername, posttype -->
                @using (Html.BeginForm("LogInUser", "Authentication", FormMethod.Post))
                {
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })

                    <div class="mb-3">
                        @Html.LabelFor(m => m.Email)
                        @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
                    </div>

                    <div class="mb-3">
                        @Html.LabelFor(m => m.Password)
                        @Html.PasswordFor(m => m.Password, new { @class = "form-control" })
                    </div>

                    <button type="submit" class="btn btn-primary w-100">Login</button>
                }
            </div>
        </div>
    </div>
</div>
